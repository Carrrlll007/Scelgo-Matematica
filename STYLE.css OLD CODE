* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

/* NEW COLOR PALETTE & FONT */
body {
  font-family: "Poppins", sans-serif;
  background: #1D2D44; /* Darker, softer study-room background */
  color: #EAEAEA;
}

/* NAVBAR */
.navbar {
  position: sticky;
  top: 0;
  z-index: 10;
  /* Use the new background */
  background: rgba(29, 45, 68, 0.95);
  backdrop-filter: blur(6px);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 60px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.15);
}

.logo {
  font-size: 1.6rem;
  font-weight: 700;
}

.logo span {
  color: #FFC300; /* Primary highlight color (warm gold/orange) */
}

.nav-links {
  display: flex;
  gap: 24px;
}

.nav-links a {
  color: #ffffff;
  text-decoration: none;
  font-size: 0.95rem;
  font-weight: 500;
  position: relative;
}

.nav-links a::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: -4px;
  width: 0%;
  height: 2px;
  background: #FFC300; /* Highlight color for nav links */
  transition: width 0.2s ease;
}

.nav-links a:hover::after {
  width: 100%;
}

/* SECTIONS */
.section {
  padding: 70px 60px;
}

.section-light {
  background: #F4F4F9; /* Light paper/desk color for light sections */
  color: #333333; /* Darker text for contrast */
}

.section-dark {
  background: #1D2D44; /* Use the body background */
  color: #fff;
}

.container {
  max-width: 1100px;
  margin: 0 auto;
}

/* HERO */
.hero {
  min-height: 70vh;
  display: flex;
  align-items: center;
  justify-content: center;
  /* Use a gradient of the brand colors */
  background: radial-gradient(circle at top, #2C4263 0, #1D2D44 65%);
}

.hero-content {
  max-width: 800px;
  text-align: center;
}

.hero-content h1 {
  font-size: 2.4rem;
  margin-bottom: 18px;
}

/* NEW TAGLINE STYLE */
.tagline {
  font-size: 1.2rem;
  font-weight: 500;
  color: #FFC300;
  margin-bottom: 25px;
  font-style: italic;
  opacity: 0.95;
}

.hero-content p {
  font-size: 1rem;
  line-height: 1.7;
  opacity: 0.9;
  margin-bottom: 28px;
}

.btn-primary {
  display: inline-block;
  padding: 12px 26px;
  border-radius: 999px;
  background: #FFC300; /* Use the brand highlight color */
  color: #1D2D44; /* Dark button text */
  text-decoration: none;
  font-weight: 700;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5); /* Less intense shadow */
  transition: background 0.3s ease;
}

.btn-primary:hover {
    background: #FFD700;
}

.btn-primary.small-btn {
  padding: 10px 22px;
  font-size: 0.9rem;
}

/* TITLES */
.section-light h2,
.section-dark h2 {
  font-size: 2rem;
  margin-bottom: 24px;
}

/* ABOUT LAYOUT */
.two-columns {
  display: grid;
  grid-template-columns: 3fr 2fr;
  gap: 40px;
}

.section-light p {
  line-height: 1.7;
  margin-bottom: 16px;
  color: #333333;
}

.about-box {
  border-radius: 14px;
  padding: 18px 22px;
  background: #ffffff;
  box-shadow: 0 12px 24px rgba(0, 0, 0, 0.08);
}

.about-box h3 {
  margin-bottom: 10px;
  color: #1D2D44;
}

.about-box ul {
  list-style: none;
  color: #333333;
}

.about-box li {
  margin-bottom: 6px;
}

/* SCELGO MATEMATICA */
.intro {
  margin-bottom: 28px;
  line-height: 1.7;
}

.intro a {
  color: #FFC300; /* Use the brand highlight color */
}

.topics-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
  gap: 20px;
  margin-bottom: 30px;
}

.topic-card {
  border-radius: 14px;
  background: #2C4263; /* Slightly lighter background for the cards */
  padding: 18px 20px;
  box-shadow: 0 16px 35px rgba(0, 0, 0, 0.6);
}

.topic-card h3 {
  margin-bottom: 12px;
  color: #FFC300; /* Brand highlight color for topic titles */
}

.topic-card ul {
  list-style: disc;
  padding-left: 18px;
  font-size: 0.92rem;
}

.topic-card li + li {
  margin-top: 4px;
}

.footer-text {
  text-align: center;
  font-size: 0.9rem;
}

.footer-text a {
  color: #FFC300;
}

/* ESERCIZI & AGENT */
.agent-area {
  display: grid;
  grid-template-columns: 1.2fr 1.8fr;
  gap: 30px;
  align-items: flex-start;
  margin-bottom: 40px;
}

.agent-text h3 {
  margin-bottom: 10px;
}

.agent-text p {
  line-height: 1.7;
}

.agent-form {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.agent-form label {
  font-weight: 500;
}

.agent-form textarea {
  min-height: 120px;
  border-radius: 10px;
  border: 1px solid #ccc;
  padding: 10px 12px;
  resize: vertical;
  font-family: "Poppins", sans-serif;
  font-size: 0.95rem;
  color: #333;
}

.agent-form select { /* NEW STYLE FOR MODE SELECTOR */
    padding: 10px 12px;
    border-radius: 10px;
    border: 1px solid #ccc;
    background: #fff;
    font-size: 0.95rem;
    color: #333;
}

.agent-output {
  margin-top: 10px;
  padding: 15px; /* Increased padding */
  border-radius: 10px;
  background: #ffffff;
  border: 1px solid #ddd;
  font-size: 0.95rem;
  line-height: 1.6;
  color: #333;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

.ex-subtitle {
  margin-bottom: 16px;
  font-size: 1.2rem;
}

.exercise-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: 20px;
}

.exercise-card {
  background: #ffffff;
  border-radius: 14px;
  padding: 18px 20px;
  box-shadow: 0 12px 24px rgba(0, 0, 0, 0.08);
  font-size: 0.95rem;
  color: #111;
}

.exercise-card h4 {
  margin-bottom: 10px;
}

.exercise-card ol {
  padding-left: 18px;
}

.exercise-card li + li {
  margin-top: 6px;
}

/* FOOTER */
.footer {
  text-align: center;
  padding: 16px 12px 24px;
  font-size: 0.8rem;
  background: #14213d; /* Very dark blue for a solid base */
  color: #bbbbbb;
}

/* RESPONSIVE */
@media (max-width: 768px) {
  .navbar {
    padding: 10px 18px;
    flex-wrap: wrap;
    gap: 8px;
  }

  .section {
    padding: 50px 18px;
  }

  .two-columns {
    grid-template-columns: 1fr;
  }

  .hero-content h1 {
    font-size: 1.9rem;
  }

  .agent-area {
    grid-template-columns: 1fr;
  }
}
